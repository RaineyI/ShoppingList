<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.raineyi.shoppinglist">


    <application
        android:name=".presentation.ShopApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_shop_list"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_shop_list"
        android:supportsRtl="true"
        android:theme="@style/Theme.ShoppingList"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ShopItemActivity"
            android:exported="false"
            android:screenOrientation="fullSensor"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".data.provider.ShopListProvider"
            android:authorities="com.raineyi.shoppinglist"
            android:exported="true" />
    </application>
    <queries>
        <provider android:authorities="com.raineyi.shoppinglist"/>
    </queries>
</manifest>